let os = import('std/os');
let vec = import('std/vec');
let sys = import('std/sys');
let builder = import('std/builder');

let build = builder.new().make_dll();

build.add_src('src/term.cpp');

sys.exit(build.perform('term'));